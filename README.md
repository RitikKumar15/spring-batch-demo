This is a spring batch application demonstrating data migration from flat file to h2 database for learning purpose.

Pre-requisites
jdk 17 or above
mvn 4 or above

Note: This application has authentication enabled with JWT bearer token so we have to add following env variable before
running the application.

jwt_secrete_key
db_username
db_password

You can run the application using in-built ide java configuration
